<UserControl x:Class="CodeAlong.WPF.View.TopicsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1200">
    <!-- Volume/Topic View -->
    <UserControl.Resources>
        <DataTemplate x:Key="CustomerDataTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Title,ValidatesOnNotifyDataErrors=False}" FontWeight="Bold"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Grid KeyDown="VolumeItem_KeyDown">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Volume list -->
        <Grid Grid.Column="0"
              Width="400"
              Background="#AAA">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel>
                <GroupBox Header="S_earch" Margin="6,0" Padding="0">
                    <StackPanel Height="50">
                        <TextBox Text="{Binding FilterString}"
                                 DockPanel.Dock="Left"
                                 MinWidth="260"
                                 Height="27"
                                 HorizontalContentAlignment="Stretch"
                                 Name="SearchInput"
                                 KeyDown="SearchInput_KeyDown"
                                 TextAlignment="Justify" />
                        <StackPanel Orientation="Horizontal">
                            <Button HorizontalAlignment="Left" Content="_Clear" Width="75" Margin="0 3 0 0" Click="ClearFilter_Click" ToolTip="Clear Search Text" Command="{Binding ClearFilterCommand}" />
                            <Button HorizontalAlignment="Left" Content="_JSON" Width="75" Margin="0 3 0 0" Click="ViewJson_Click" ToolTip="Open JSON File" />
                        </StackPanel>
                    </StackPanel>
                </GroupBox>
                <StackPanel Orientation="Horizontal">
                    <Button Margin="10" Width="75" Command="{Binding AddCommand}" Click="AddNewItem_Click">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/Images/add.png" Height="18" Margin="0 0 5 0"/>
                            <TextBlock Text="Add"/>
                        </StackPanel>
                    </Button>
                    <Button Content="Delete" Width="75" Margin="0 10 10 10"  Command="{Binding DeleteCommand}" />
                </StackPanel>
            </StackPanel>
            <ListView ItemsSource="{Binding Path=ListViewItems}"
                      SelectedItem="{Binding SelectedItem,Mode=TwoWay}"
                      ItemTemplate="{StaticResource CustomerDataTemplate}"
                      Grid.Row="1" Margin="10 0 10 10">
            </ListView>
        </Grid>

        <!-- Volume detail -->
        <ScrollViewer Grid.Column="1" Margin="0 0 10 0" Visibility="{Binding IsItemSelected,Converter={StaticResource BooleanToVisibilityConv}}">
            <StackPanel Margin="20 0 20 0">
                <Label>Title:</Label>
                <TextBox Text="{Binding SelectedItem.Title, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Name="Title"/>
                <StackPanel Margin="0 8 0 0" Orientation="Horizontal">
                    <Label Width="80">Created:</Label>
                    <TextBox Width="150" IsReadOnly="True" Text="{Binding SelectedItem.Created, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Width="80" Margin="20 0 0 0">LastModified:</Label>
                    <TextBox Width="150" IsReadOnly="True" Text="{Binding SelectedItem.LastModified, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Margin="0 20 0 0 ">
                    <Button Content="Save" Width="75" Margin="0 10 10 10"  Command="{Binding SaveCommand}" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
