<UserControl x:Class="CodeAlong.WPF.View.References.ReferenceView_Hfdp"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewsections="clr-namespace:CodeAlong.WPF.View.Sections"
             xmlns:vmsections="clr-namespace:CodeAlong.WPF.ViewModel.Sections"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1200">
    <UserControl.Resources>
        <DataTemplate x:Key="CustomerDataTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Title,ValidatesOnNotifyDataErrors=False}" FontWeight="Bold"/>
            </StackPanel>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vmsections:ViewModelStrategyPattern}">
            <viewsections:StrategyPatternView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vmsections:ViewModelObserverPattern}">
            <viewsections:ObserverPatternView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vmsections:ViewModelDecorator}">
            <viewsections:DecoratorView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vmsections:ViewModelFactoryPattern}">
            <viewsections:FactoryPatternView/>
        </DataTemplate>
    </UserControl.Resources>
    
    <Grid KeyDown="MainItem_KeyDown">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0"
              Width="280"
              Background="#AAA">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel Visibility="Collapsed">
                <StackPanel Orientation="Horizontal">
                    <Button Margin="10" Width="75"
                            Command="{Binding AddCommand}"
                            Click="AddNewItem_Click">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="/Images/add.png" Height="18" Margin="0 0 5 0"/>
                            <TextBlock Text="Add"/>
                        </StackPanel>
                    </Button>
                    <Button Content="Delete" Width="75" Margin="0 10 10 10"  Command="{Binding DeleteCommand}" />
                    <Button HorizontalAlignment="Left" Content="_JSON" Width="75" Margin="0 10 10 10" Click="ViewJson_Click" ToolTip="Open JSON File" />
                </StackPanel>
                <ComboBox ItemsSource="{Binding Path=ListViewItems}"
                      SelectedItem="{Binding SelectedItem,Mode=TwoWay}"
                      ItemTemplate="{StaticResource CustomerDataTemplate}"
                      Grid.Row="1" Margin="10 0 10 10">
                </ComboBox>
                
                <!-- item detail -->
                <GroupBox Header="Reference" Margin="10 0" Visibility="{Binding IsItemSelected,Converter={StaticResource BooleanToVisibilityConv}}">
                    <StackPanel Margin="10 0">
                        <StackPanel>
                            <Label HorizontalAlignment="Left" Width="100">Title:</Label>
                            <TextBox Width="500" Text="{Binding SelectedItem.Title, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Name="Title"/>
                            <StackPanel Margin="0 10">
                                <Label HorizontalAlignment="Left" Width="100">Description:</Label>
                                <TextBox Width="500" Text="{Binding SelectedItem.Description, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel>
                            <Button Content="Save" Width="75" Margin="0 10 10 10"  Command="{Binding SaveCommand}" />
                        </StackPanel>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
            <GroupBox Grid.Row="1"
                      Margin="10"
                      Header="Sections"
                      VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch">
                <StackPanel Margin="10">
                    <Button Width="210" FontSize="20" 
                            Click="ButtonSection_Click" 
                            Name="ButtonSectionStrategy" HorizontalContentAlignment="Left" Padding="10,0,0,0"
                            Command="{Binding SelectSectionCommand}"
                            CommandParameter="{Binding StrategyPattern}">1. _Strategy Pattern</Button>
                    <Button Width="210" Margin="0 10 0 0" FontSize="20"
                            Click="ButtonSection_Click" 
                            Name="ButtonSectionObserver" HorizontalContentAlignment="Left" Padding="10,0,0,0"
                            Command="{Binding SelectSectionCommand}"
                            CommandParameter="{Binding ObserverPattern}">2. Obse_rver Pattern</Button>
                    <Button Width="210" Margin="0 10 0 0" FontSize="20"
                            Click="ButtonSection_Click" 
                            Name="ButtonSectionDecorator" HorizontalContentAlignment="Left" Padding="10,0,0,0"
                            Command="{Binding SelectSectionCommand}"
                            CommandParameter="{Binding DecoratorPattern}">3. _Decorator Pattern</Button>
                    <Button Width="210" Margin="0 10 0 0" FontSize="20"
                            Click="ButtonSection_Click" 
                            Name="ButtonSectionFactory"  HorizontalContentAlignment="Left" Padding="10,0,0,0"
                            Command="{Binding SelectSectionCommand}"
                            CommandParameter="{Binding FactoryPattern}">4. _Factory Pattern</Button>
                </StackPanel>
            </GroupBox>
        </Grid>
        <!-- section detail -->
        <ScrollViewer Grid.Row="1" Grid.Column="1" Margin="0 0 10 0">
            <ContentControl Content="{Binding SelectedViewModel}"/>
        </ScrollViewer>
    </Grid>
</UserControl>
