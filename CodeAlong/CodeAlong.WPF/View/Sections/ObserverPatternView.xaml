<UserControl x:Class="CodeAlong.WPF.View.Sections.ObserverPatternView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CodeAlong.WPF.View.Sections"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" HorizontalAlignment="Left" Margin="10">
            <Label HorizontalAlignment="Left" Width="100">Title:</Label>
            <TextBox Width="500" Text="{Binding Title, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Name="Title"/>
        </StackPanel>

        <GroupBox Grid.Row="1" Header="Weather Measurements" Margin="10" Padding="10">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Width="100">Temperature (°C):</Label>
                    <TextBox Width="200" Text="{Binding Temperature, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Width="100">Humidity (%):</Label>
                    <TextBox Width="200" Text="{Binding Humidity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Width="100">Pressure:</Label>
                    <TextBox Width="200" Text="{Binding Pressure, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <Button Command="{Binding UpdateMeasurementsCommand}" Content="Update Measurements" Width="150" Margin="0,10" HorizontalAlignment="Left"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Observer Management" Margin="10" Padding="10">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Width="100">Observer Type:</Label>
                    <TextBox Width="200" Text="{Binding NewObserverName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                    <Button Command="{Binding AddObserverCommand}" Content="Add Observer" Width="120" Margin="0,0,10,0"/>
                    <Button Command="{Binding ToggleObserverCommand}" Content="Toggle Selected" Width="120"/>
                </StackPanel>
                <Label Margin="0,10,0,5">Active Observers:</Label>
                <ListBox ItemsSource="{Binding Observers}" SelectedItem="{Binding SelectedObserver}" Height="100" Margin="0,0,0,5">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayInfo}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Weather Displays" Margin="10" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Observers}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="LightGray" BorderThickness="1" Margin="5" Padding="5" CornerRadius="3">
                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding Display}" FontSize="14"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>
