<UserControl x:Class="CodeAlong.WPF.View.Sections.StrategyPatternView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CodeAlong.WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Left" Margin="10">
            <Label HorizontalAlignment="Left" Width="100">Title:</Label>
            <TextBox Width="500" Text="{Binding Title, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Name="Title"/>

            <GroupBox Header="Add Duck" Margin="0,10,0,0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <ComboBox Width="150" ItemsSource="{Binding DuckTypes}" SelectedItem="{Binding SelectedDuckType}" DisplayMemberPath="Key"/>
                        <Button Margin="10 0"
                                Height="25"
                                Width="80"
                                Command="{Binding AddCommand}">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="/Images/add.png" Height="18" Margin="0 0 5 0"/>
                                <TextBlock Text="Add"/>
                            </StackPanel>
                        </Button>                        
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <StackPanel Margin="0,0,20,0">
                            <Label>Fly Behavior</Label>
                            <RadioButton Content="Fly with Wings" GroupName="FlyBehavior" IsChecked="{Binding FlyWithWings}"/>
                            <RadioButton Content="Cannot Fly" GroupName="FlyBehavior" IsChecked="{Binding FlyNoWay}"/>
                            <RadioButton Content="Rocket Powered" GroupName="FlyBehavior" IsChecked="{Binding FlyRocketPowered}"/>
                        </StackPanel>
                        <StackPanel>
                            <Label>Quack Behavior</Label>
                            <RadioButton Content="Normal Quack" GroupName="QuackBehavior" IsChecked="{Binding QuackDefault}"/>
                            <RadioButton Content="Mute" GroupName="QuackBehavior" IsChecked="{Binding QuackMute}"/>
                            <RadioButton Content="Squeak" GroupName="QuackBehavior" IsChecked="{Binding QuackSqueak}"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Duck Order" Margin="0,10,0,0">
                <StackPanel>
                    <ListBox Name="DucksListBox" 
                             ItemsSource="{Binding DuckOrder.Ducks}" 
                             SelectedItem="{Binding SelectedDuck}"
                             Height="250">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding DuckType}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding FlyInfo}" Margin="10,0,0,0"/>
                                    <TextBlock Text="{Binding QuackInfo}" Margin="10,0,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <Button Content="Remove Selected" Command="{Binding RemoveCommand}" Margin="0,0,5,0" Width="120"/>
                        <Button Content="Clear Order" Command="{Binding ClearOrderCommand}" Width="100"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Label Content="Total Ducks:" FontWeight="Bold" FontSize="14"/>
                        <TextBlock Text="{Binding DuckCount}" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Edit Duck" Margin="0,10,0,0" Visibility="{Binding EditDuckVisibility}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <StackPanel Margin="0,0,20,0">
                            <Label>Change Fly Behavior</Label>
                            <RadioButton Content="Fly with Wings" GroupName="EditFlyBehavior" IsChecked="{Binding EditFlyWithWings}"/>
                            <RadioButton Content="Cannot Fly" GroupName="EditFlyBehavior" IsChecked="{Binding EditFlyNoWay}"/>
                            <RadioButton Content="Rocket Powered" GroupName="EditFlyBehavior" IsChecked="{Binding EditFlyRocketPowered}"/>
                        </StackPanel>
                        <StackPanel>
                            <Label>Change Quack Behavior</Label>
                            <RadioButton Content="Normal Quack" GroupName="EditQuackBehavior" IsChecked="{Binding EditQuackDefault}"/>
                            <RadioButton Content="Mute" GroupName="EditQuackBehavior" IsChecked="{Binding EditQuackMute}"/>
                            <RadioButton Content="Squeak" GroupName="EditQuackBehavior" IsChecked="{Binding EditQuackSqueak}"/>
                        </StackPanel>
                    </StackPanel>
                    <Button Content="Update Behaviors" 
                            Command="{Binding UpdateBehaviorsCommand}" 
                            Margin="5"
                            Width="150"
                            HorizontalAlignment="Left"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>
