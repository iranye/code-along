<UserControl x:Class="CodeAlong.WPF.View.Sections.DecoratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CodeAlong.WPF.View.Sections"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Left" Margin="10">
            <Label HorizontalAlignment="Left" Width="100">Title:</Label>
            <TextBox Width="500" Text="{Binding Title, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Name="Title"/>

            <GroupBox Header="Create Beverage" Margin="0,10,0,0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <ComboBox Width="150" ItemsSource="{Binding BeverageTypes}" SelectedItem="{Binding SelectedBeverageType}" DisplayMemberPath="Key"/>
                        <ComboBox Width="100" Margin="10,0,0,0" ItemsSource="{Binding Sizes}" SelectedItem="{Binding SelectedSize}"/>
                        <Button Margin="10 0"
                                Height="25"
                                Width="80"
                                Command="{Binding AddCommand}">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="/Images/add.png" Height="18" Margin="0 0 5 0"/>
                                <TextBlock Text="Add"/>
                            </StackPanel>
                        </Button>                        
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <StackPanel>
                            <Label>Condiments</Label>
                            <CheckBox Content="Mocha" IsChecked="{Binding CondimentMocha}"/>
                            <CheckBox Content="Whip" IsChecked="{Binding CondimentWhip}"/>
                            <CheckBox Content="Soy" IsChecked="{Binding CondimentSoy}"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Order" Margin="0,10,0,0">
                <StackPanel>
                    <ListBox ItemsSource="{Binding CoffeeOrder.Beverages}" DisplayMemberPath="GetInfo" Height="250"/>
                    <Button Content="Clear Order" Command="{Binding ClearOrderCommand}" Margin="0,5,0,0"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>
